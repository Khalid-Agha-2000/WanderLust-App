<% layout("layouts/boilerplate") -%>

<div class="row mt-3">
    <div class="col-8 offset-2">
        <h3 class="mt-4"><%= listing.title %></h3>
    </div>
    <div class="show-card card col-8 offset-2">
        <div class="show-card-body card w-100" style="width: 18rem;">
            <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="Listing Image">
            <div class="card-body">
                <b><p class="card-text"></b><br>
                    <i>Listing by: <b><%= listing.owner.username %> </b></i>
                    <br><br>
                    <h4>Listing Description</h4>
                    <%= listing.description %><br>
                    <br><br>
                    &#8360;: <%= listing.price.toLocaleString("in-IN") %><br>
                    <br>
                    <p>
                        <%= listing.location %>,
                        <%= listing.country %>
                    </p>
                    <hr>
                </p>

            </div>
        </div>
        <% if(currUser && currUser._id.equals(listing.owner._id)) { %>
            <div class="col-1 btns">
                <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark edit">Edit</a><br>
                <form action="/listings/<%= listing.id %>?_method=DELETE" method="POST">
                    <button class="btn btn-dark offset-1 delete">Delete</button>
                </form>
            </div>
            <br><br>
        <% } %>

        <% if(currUser) {%>
            <div>
                <h4 class="col-8">Leave a Review</h4><br>
                <form action="/listings/<%= listing.id %>/reviews" novalidate class="needs-validation" method="POST">
                    <div class="col-8 mb-3">
                        <label for="rating">Rating</label>
                        <input type="range" class="form-range" name="review[rating]" id="" min="1" max="5">
                    </div>
                    <div class="col-8 mb-3">
                        <label for="comment">Comments</label>
                        <textarea required name="review[comment]" id="comment" rows="10" cols="100"></textarea>
                        <div class="invalid-feedback">Please enter some comment for review</div>
                    </div>
                    <button class="btn btn-outline-dark">Submit</button>
                </form>
                <br>
            <hr>
            <% } %>
            <br>

            <p><b>All Reviews</b></p>
            <div class="row">
                <% for(review of listing.reviews) { %>
                    <div class="card mb-2 p-2 col-5 ms-3 review-card">
                        <div class="card-body">
                            <h6 class="card-title"><%= review.author.username %></h6>
                            <p class="card-text"><%= review.comment %></p>
                            <p class="card-text"><%= review.rating %>Stars</p>
                        </div>
                        <form method="POST" action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE">
                           <button class="btn btn-sm col-3 mb-1 btn-dark">Delete</button> 
                        </form>
                    </div>
                <%}%>
            </div>
            
        </div>
    </div>
</div>